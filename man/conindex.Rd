% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conindex.R
\name{conindex}
\alias{conindex}
\title{Rank-dependent inequality (concentration) indices}
\usage{
conindex(
  data,
  outcome,
  rankvar = NULL,
  weights = NULL,
  subset = NULL,
  cluster = NULL,
  robust = FALSE,
  truezero = FALSE,
  limits = NULL,
  generalized = FALSE,
  bounded = FALSE,
  wagstaff = FALSE,
  erreygers = FALSE,
  v = NULL,
  beta = NULL,
  compare = NULL,
  by = NULL,
  keeprank = FALSE,
  svy = FALSE,
  design = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome}{String naming the outcome variable of interest (\code{h}).}

\item{rankvar}{String naming the ranking (socioeconomic) variable. If \code{NULL}
(or equal to \code{outcome}), observations are ranked by \code{outcome} and the index
is a Gini coefficient.}

\item{weights}{String naming a weights variable, or \code{NULL} for equal weights.}

\item{subset}{Optional logical vector (length \code{nrow(data)}) selecting the
estimation sample (the analogue of Stata \code{if}).}

\item{cluster}{String naming a cluster variable for clustered standard errors.}

\item{robust}{Logical; request Huber/White/sandwich (HC1) standard errors.}

\item{truezero}{Logical; declare the outcome ratio-scaled (fixed zero),
yielding the standard concentration index.}

\item{limits}{Numeric vector giving the theoretical minimum (and, for bounded
variables, maximum): \code{limits = min} or \code{limits = c(min, max)}. Use \code{NA} for a
missing bound.}

\item{generalized}{Logical; request the generalized (absolute) index. Requires
\code{truezero}.}

\item{bounded}{Logical; the outcome is bounded. Requires \code{limits = c(min, max)}.}

\item{wagstaff, erreygers}{Logical; Wagstaff / Erreygers normalisation for
bounded variables (with \code{bounded} and \code{limits}).}

\item{v}{Numeric > 1; the extended-index distributional-sensitivity parameter.
Requires \code{truezero}.}

\item{beta}{Numeric > 1; the symmetric-index sensitivity parameter. Requires
\code{truezero}.}

\item{compare}{String naming a group variable across which to compare the index
and test homogeneity.}

\item{by}{String naming a variable; the index is computed \strong{separately} within
each level of \code{by} (the analogue of the Stata \verb{by varlist:} prefix), returning
one result per group. Cannot be combined with \code{compare}.}

\item{keeprank}{Logical; if \code{TRUE} the fractional rank is returned in the result
(element \code{rank}).}

\item{svy}{Logical; use a complex survey design (experimental — see Details).}

\item{design}{A \code{\link[survey:svydesign]{survey::svydesign()}} object, required when \code{svy = TRUE}.}
}
\value{
An object of class \code{conindex} (see \code{\link[=print.conindex]{print.conindex()}}) with elements
including \code{value} (the index), \code{se}, \code{n}, \code{nunique}, \code{type}, and — with
\code{compare} — a \code{comparison} component.
}
\description{
\code{conindex()} computes a range of rank-dependent inequality indices, including
the Gini coefficient, the concentration index (CI), the generalized
concentration index, the modified concentration index, the Wagstaff and
Erreygers normalised indices for bounded variables, and the distributionally
sensitive extended and symmetric indices (and their generalized versions).
It is a faithful R translation of the Stata command \code{conindex} (O'Donnell et
al. 2016).
}
\details{
There is no default index: the options select which index is computed, exactly
as in the Stata command. See the vignette and the examples below.

Standard errors reproduce Stata's \code{regress} variance estimators: HC1 for
\code{robust}, cluster-robust (HC1 with the small-sample cluster adjustment) for
\code{cluster}. Standard errors are \strong{not} produced for the extended and symmetric
indices (matching the original). The \code{svy} path is experimental: it fits the
convenient-covariance regression with \code{\link[survey:svyglm]{survey::svyglm()}} and has not been
validated against the Stata oracle.
}
\examples{
set.seed(1)
df <- data.frame(
  h = rgamma(500, 2, 2),
  ses = rnorm(500),
  wt = runif(500, 0.5, 1.5)
)
# Standard concentration index
conindex(df, "h", rankvar = "ses", truezero = TRUE)
# Gini coefficient (rank by the outcome itself)
conindex(df, "h", truezero = TRUE)
}
\references{
O'Donnell, O., O'Neill, S., Van Ourti, T. & Walsh, B. (2016). conindex:
Estimation of concentration indices. \emph{The Stata Journal}.
}
