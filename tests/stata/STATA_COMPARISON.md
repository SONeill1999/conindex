# Stata <-> R test correspondence (conindex)

Generated by `tests/stata/compare_stata.R` against Stata 18 via RStata on 2026-07-10.

Each row runs the Stata command from `Stata/Example do file.do` live and
compares `r(CI)` / `r(CIse)` with the matching R `conindex()` call.
Relative differences at the 1e-9 level or below are floating-point identical.

| Stata test (id) | R equivalent | index | CI rel.diff | SE rel.diff | PASS |
|---|---|---|---|---|---|
| `hh_CI` | `conindex(hh, ...)` | rankvar(wealthindex) truezero cluster(PSU) | 2.7755576e-15 | 2.0677904e-15 | PASS |
| `hh_GenCI` | `conindex(hh, ...)` | rankvar(wealthindex) generalized truezero cluster(PSU) | 1.4511402e-14 | 2.9390825e-14 | PASS |
| `hh_Ext_v1.5` | `conindex(hh, ...)` | rankvar(wealthindex) v(1.5) truezero cluster(PSU) | 7.1253786e-10 |         0 | PASS |
| `hh_Sym_b5` | `conindex(hh, ...)` | rankvar(wealthindex) beta(5) truezero cluster(PSU) | 1.6570582e-09 |         0 | PASS |
| `kids_CI_u1mr` | `conindex(kids, ...)` | rankvar(wealthindex) truezero cluster(PSU) | 2.3808736e-10 | 4.921465e-11 | PASS |
| `kids_CI_u1sr` | `conindex(kids, ...)` | rankvar(wealthindex) truezero cluster(PSU) | 1.5769917e-11 | 3.1257507e-12 | PASS |
| `kids_GenCI_u1mr` | `conindex(kids, ...)` | rankvar(wealthindex) generalized truezero cluster(PSU) | 1.4805945e-11 | 2.9391355e-12 | PASS |
| `kids_Erreygers` | `conindex(kids, ...)` | rankvar(wealthindex) erreygers bounded limits(0 1) cluster(PSU) | 5.9223779e-11 | 1.1756522e-11 | PASS |
| `kids_Wagstaff` | `conindex(kids, ...)` | rankvar(wealthindex) wagstaff bounded limits(0 1) cluster(PSU) | 2.5387617e-10 | 5.2340573e-11 | PASS |
| `kids_Ext_v1.5` | `conindex(kids, ...)` | rankvar(wealthindex) truezero v(1.5) cluster(PSU) | 8.827003e-10 |         0 | PASS |
| `kids_Sym_b1.5` | `conindex(kids, ...)` | rankvar(wealthindex) truezero beta(1.5) cluster(PSU) | 1.2570968e-09 |         0 | PASS |
| `kids_GenExt_v1.5` | `conindex(kids, ...)` | rankvar(wealthindex) generalized truezero v(1.5) cluster(PSU) | 4.2390688e-11 |         0 | PASS |
| `kids_GenSym_b5` | `conindex(kids, ...)` | rankvar(wealthindex) generalized truezero beta(5) cluster(PSU) | 2.4445432e-10 |         0 | PASS |

## compare(urban) — Erreygers, group homogeneity tests

| statistic | Stata | R | rel.diff | PASS |
|---|---|---|---|---|
| F | 0.66985873 | 0.66985877 | 3.6811777e-08 | PASS |
| CI0 | -0.029852744 | -0.029852744 | 1.1514387e-10 | PASS |
| CI1 | -0.028699794 | -0.028699794 | 3.1250931e-10 | PASS |
| z | 0.092970152 | 0.092970117 | 3.4789336e-08 | PASS |

**Overall: ALL PASS**

## Coverage and gaps (missing tests)

The cases above span every index *type* in the package. The following items
from the Stata materials are **not** directly compared here:

* **`bys urban: conindex ...`** (by-prefix, `Example do file.do` line 68). The R
  port has no `by:` equivalent; users split the data and call `conindex()` per
  group. The per-group values equal the `compare()` group values (which are
  cross-checked above), so this is covered indirectly but not via a `by:` call.
* **`graph`** (Lorenz / concentration curves) and **`keepgraphdata`** — visual
  output with no numeric oracle in the `.smcl`; not cross-checked.
* **`keeprank`** — the returned fractional rank is unit-tested in R
  (`test-indices.R`) but not compared against a Stata-saved rank variable.
* **`svy`** — experimental in the R port; the example do-file contains no `svy`
  run, so there is no oracle to compare against.
* **Plain `robust` and classical (non-cluster) SEs** — every example uses
  `cluster(PSU)`; HC1 robust and classical OLS SEs are exercised only by the R
  unit tests, not cross-checked against Stata here.
* **`fweight`** — only `aweight`/`pweight` appear in the examples; integer
  frequency weights are not cross-checked.
* **Redundant permutations** (e.g. `healthexp v(5)`, `u1sr` variants of every
  index) are omitted because they duplicate an index type already verified.
